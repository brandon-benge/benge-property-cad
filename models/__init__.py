"""Focused geometry and metadata collections for the property model.

Modules in this package contribute ``DesignElement`` instances to the one
``DesignModel`` composed by the parent project's ``model.py`` entry point.
"""
