# Skills

Only two managed skills are provided:

- `general-contractor/` supports read-only review of generated outputs and delegation to the Architect.
- `python-cad-architect/` supports Python-first CAD implementation, standards exports, validation, and tests.

The General Contractor must not load the Architect skill. OpenCode permissions expose each skill only to its matching agent.
