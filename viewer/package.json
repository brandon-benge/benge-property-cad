{
  "name": "python-cad-artifact-viewer",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prepare-model": "node scripts/prepare-model.mjs",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:model": "node scripts/verify-prepared-model.mjs",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@babylonjs/core": "^9.16.2",
    "@babylonjs/loaders": "^9.16.2"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@webgpu/types": "^0.1.71",
    "jsdom": "^26.1.0",
    "typescript": "^5.8.3",
    "vite": "^7.0.0",
    "vitest": "^3.2.4"
  }
}
